databases:
  - name: sdhero-db
    plan: free

services:
  - type: web
    name: sdhero-service
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: sdhero-db
          property: connectionString
      - key: ADMIN_PASSWORD
        value: 318eklavya
      - key: JWT_SECRET
        value: super_secret_key_sdhero_patna
